# testing

Starter Python project managed with `uv`.

## Quick start

1. Install dependencies:
   - `uv sync`
2. Run the app:
   - `uv run testing`
3. Run tests:
   - `uv run pytest`

## Repository bootstrap defaults

- License: MIT
- CI: GitHub Actions (`.github/workflows/ci.yml`)
- Dependency updates: Renovate (`renovate.json`)
- Git hooks: pre-commit with TruffleHog secret scanning


test
