services:

  # Local services are available on their ports, but also available on:
  # http://api.localhost.tiangolo.com: backend
  # http://dashboard.localhost.tiangolo.com: frontend
  # etc. To enable it, update .env, set:
  # DOMAIN=localhost.tiangolo.com
  proxy:
    image: traefik:v3.7
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "127.0.0.1:80:80"
      - "127.0.0.1:8090:8080"
    # Duplicate the command from compose.yml to add --api.insecure=true
    command:
      # Enable Docker in Traefik, so that it reads labels from Docker services
      - --providers.docker
      # Add a constraint to only use services with the label for this stack
      - --providers.docker.constraints=Label(`traefik.constraint-label`, `traefik-public`)
      # Do not expose all Docker services, only the ones explicitly exposed
      - --providers.docker.exposedbydefault=false
      # Create an entrypoint "http" listening on port 80
      - --entrypoints.http.address=:80
      # Create an entrypoint "https" listening on port 443
      - --entrypoints.https.address=:443
      # Enable the access log, with HTTP requests
      - --accesslog
      # Enable the Traefik log, for configurations and errors
      - --log
      # Enable debug logging for local development
      - --log.level=DEBUG
      # Enable the Dashboard and API
      - --api
      # Enable the Dashboard and API in insecure mode for local development
      - --api.insecure=true
    labels:
      # Enable Traefik for this service, to make it available in the public network
      - traefik.enable=true
      - traefik.constraint-label=traefik-public
      # Dummy https-redirect middleware that doesn't really redirect, only to
      # allow running it locally
      - traefik.http.middlewares.https-redirect.contenttype.autodetect=false
    networks:
      - traefik-public
      - default

  db:
    restart: "no"
    ports:
      - "127.0.0.1:5432:5432"
    env_file:
      - .env.local

  adminer:
    restart: "no"
    ports:
      - "127.0.0.1:8080:8080"

  backend:
    restart: "no"
    ports:
      - "127.0.0.1:8000:8000"
    build:
      context: .
      dockerfile: backend/Dockerfile
    # command: sleep infinity  # Infinite loop to keep container alive doing nothing
    command:
      - fastapi
      - run
      - --reload
      - "app/main.py"
    develop:
      watch:
        - path: ./backend
          action: sync
          target: /app/backend
          ignore:
            - ./backend/.venv
            - .venv
        - path: ./backend/pyproject.toml
          action: rebuild
    # TODO: remove once coverage is done locally
    volumes:
      - ./backend/htmlcov:/app/backend/htmlcov
    environment:
      # Docker E2E parity: .env.local often disables registration locally; Playwright
      # sign-up / reset-password specs need the API to accept /users/signup (overrides env_file).
      USER_REGISTRATION_ENABLED: "true"
      SMTP_HOST: "mailcatcher"
      SMTP_PORT: "1025"
      SMTP_TLS: "false"
      EMAILS_FROM_EMAIL: "noreply@example.com"
    env_file:
      - .env.local

  mailcatcher:
    image: schickling/mailcatcher
    ports:
      - "127.0.0.1:1080:1080"
      - "127.0.0.1:1025:1025"

  authjs-service:
    restart: "no"
    ports:
      # Bind only to localhost so the Auth.js bridge isn't exposed to the LAN
      # in dev. The frontend running on the host can still reach it.
      - "127.0.0.1:3001:3001"

  frontend:
    restart: "no"
    ports:
      - "127.0.0.1:5173:80"
    volumes:
      # Same-origin /api/v1 → backend (Playwright chromium must not call localhost:8000).
      - ./frontend/nginx-compose-api-proxy.conf:/etc/nginx/extra-conf.d/backend-not-found.conf:ro
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        VITE_API_URL: ""
        VITE_AUTHJS_URL: http://localhost:3001
        USER_REGISTRATION_ENABLED: "true"
        NODE_ENV: development

  playwright:
    build:
      context: .
      dockerfile: frontend/Dockerfile.playwright
      args:
        - VITE_API_URL=http://backend:8000
        - VITE_AUTHJS_URL=http://localhost:3001
        - NODE_ENV=production
    ipc: host
    depends_on:
      - backend
      - mailcatcher
      - frontend
    env_file:
      - .env.local
    environment:
      - VITE_API_URL=http://backend:8000
      - MAILCATCHER_HOST=http://mailcatcher:1080
      # For the reports when run locally
      - PLAYWRIGHT_HTML_HOST=0.0.0.0
      - CI=${CI}
    volumes:
      - ./frontend/blob-report:/app/frontend/blob-report
      - ./frontend/test-results:/app/frontend/test-results
    ports:
      - 127.0.0.1:9323:9323

networks:
  traefik-public:
    # For local dev, don't expect an external Traefik network
    external: false
